- provide(:title, 'Home')
- if logged_in?
  .mb-2.mt-2.border-bottom
  = render 'post_form', post: @post
  .mb-2.mt-2.border-bottom
  = render @feed_items
  = raw pagy_bootstrap_nav(@pagy)
- else
  = render 'welcome'


 